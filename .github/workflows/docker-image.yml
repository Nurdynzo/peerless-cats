name: Docker Image CI

on:
  workflow_dispatch:
    inputs:
      password:
        description: 'Password'
        required: true
        default: ''
        type: string

jobs:

  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      if: inputs.password == secrets.PASSWORD
      run: echo "Hello"
