name: Docker Image CI

on:
  workflow_dispatch:
    inputs:
      password:
        description: 'Password'
        required: true
        default: ''
        type: string

jobs:

  build:

    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.password == secrets.PASSWORD }}
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: echo "Hello"
