name: Docker Image CI

on:
  workflow_dispatch:
    inputs:
      password:
        description: 'Password'
        required: true
        default: ''
        type: string
env:
  build_version: "123456"
  branch_version: "122324"

jobs:

  build:

    runs-on: ubuntu-latest
    if: ${{ secrets.PASSWORD == '123456' }}
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: echo "Hello"
